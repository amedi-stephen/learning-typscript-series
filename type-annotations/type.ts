// how we declare types in typescript

let a: number;
let b: string;
let c: boolean;
let d: number[] = [1, 2, 3];
let e: any[] = [1, "Write novel", true];

enum Color { Red = 1, Blue = 2, Green = 3 };

let backgroundColor = Color.Red;

b = "Hello world!";